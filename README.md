# codes_SvsB

plot_dataMC.C -> vizualize data/MC variable distributions for B mesons

plot_X.cpp  -> vizualize data/MC variable distributions for X and PSI
  ACCSELX.h-> just like ACCSEL.h but without Bnorm_svpvDistance>2, used in file plot_X.cpp

Both these produce binned results
